ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [ com.jikanhub.api.ApplicationKt.module ]
    }
}

database {
    driver = "org.postgresql.Driver"
    url = "jdbc:postgresql://185.225.233.145:5432/jikanhub_db"
    url = ${?DATABASE_URL}
    user = "jikan_admin"
    user = ${?DATABASE_USER}
    password = "jikanhub"
    password = ${?DATABASE_PASSWORD}
}
