package com.jikanhub.app.presentation.theme

import androidx.compose.ui.graphics.Color

// Surface colors — Japanese dark navy palette
val Surface = Color(0xFF0F1729)
val SurfaceVariant = Color(0xFF1A2340)
val SurfaceContainer = Color(0xFF141D33)
val SurfaceBright = Color(0xFF212B47)

// Text colors
val OnSurface = Color(0xFFE8EAF0)
val OnSurfaceVariant = Color(0xFF8B92A8)

// Priority colors
val PriorityHigh = Color(0xFFC75B39)       // Terracotta
val PriorityMedium = Color(0xFFD4A843)     // Amber
val PriorityLow = Color(0xFF5B8FA8)        // Soft blue

// Status colors
val Completed = Color(0xFF4A9E7A)          // Jade green
val Postponed = Color(0xFF9E7A4A)          // Warm brown

// Accent
val Accent = Color(0xFFC75B39)             // Terracotta
val AccentVariant = Color(0xFFE07050)

// Light theme variants
val LightSurface = Color(0xFFF5F3F0)
val LightSurfaceVariant = Color(0xFFFFFFFF)
val LightOnSurface = Color(0xFF1A1A2E)
val LightOnSurfaceVariant = Color(0xFF6B7280)
